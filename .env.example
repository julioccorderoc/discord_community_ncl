# ============================================================
# Community OS — Environment Variables
# ============================================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to git. This file (.env.example) is safe to commit.
#
# In production (Railway): set these as Environment Variables
# in the Railway service dashboard — do NOT upload a .env file.
# ============================================================

# --- Supabase ---
# Found at: supabase.com → your project → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SECRET_KEY=your-service-role-secret-key   # Use the service_role key, NOT the anon key

# --- Discord Bot ---
# Found at: discord.com/developers/applications → your app → Bot → Token
DISCORD_BOT_TOKEN=your-discord-bot-token

# --- Ticket System (EPIC-003) ---
# Right-click the role in Discord (Developer Mode must be on) to copy the ID
STAFF_ROLE_ID=123456789012345678        # Optional — ticket system disabled if not set
# Right-click the category channel to copy the ID
TICKET_CATEGORY_ID=123456789012345678   # Optional — tickets created in server root if not set

# --- AI Compliance Agent (EPIC-005) ---
# Found at: aistudio.google.com → Get API Key
GEMINI_API_KEY=your-gemini-api-key
# Model to use. Default: gemini-2.0-flash-lite (available to new API accounts)
# Other options: gemini-2.5-flash (if your key has access)
GEMINI_MODEL=gemini-2.0-flash-lite
# Seconds before /audit times out and returns an error to the user. Default: 20
GEMINI_TIMEOUT_SECONDS=20

# --- Admin Dashboard (EPIC-006) ---
# Simple password for the admin-gated section of the Streamlit dashboard.
# This is NOT a real auth system — internal tool only.
ADMIN_PASSWORD=choose-a-strong-password
